export const IOAKBLOG_NUMBER_OF_BLOCKS = "IOAKBLOG_NUMBER_OF_BLOCKS";

export const getCount = (index: number) => {
    return `IOAKBLOG_COUNT_${index}`;
}

export const getType = (index: number) => {
    return `IOAKBLOG_TYPE_${index}`;
}
